# ================================================
# PixelDrain Bypasser Docker Configuration
# ================================================

# REQUIRED: Your Cloudflare Workers domain (without subdomain)
# Example: If your workers are at pd1.myname.workers.dev, set this to "myname.workers.dev"
CLOUDFLARE_DOMAIN=your-domain.workers.dev

# OPTIONAL: Custom frontend domain for nginx routing
# If you have a custom domain pointed to this server, set it here
# Otherwise, it will be accessible via localhost:HTTP_PORT
FRONTEND_DOMAIN=pixel.localhost

# OPTIONAL: Ports for the nginx reverse proxy
# Change these if you need different ports
HTTP_PORT=8080
HTTPS_PORT=8443

# ================================================
# Cloudflare Workers Setup Required
# ================================================
# Before running this Docker setup, you need to deploy the proxy workers:
# 
# 1. Deploy proxy-worker.js to these domains:
#    - pd1.${CLOUDFLARE_DOMAIN}
#    - pd2.${CLOUDFLARE_DOMAIN}
#    - pd3.${CLOUDFLARE_DOMAIN}
#    - pd4.${CLOUDFLARE_DOMAIN}
#    - pd5.${CLOUDFLARE_DOMAIN}
#    - pd6.${CLOUDFLARE_DOMAIN}
#    - pd7.${CLOUDFLARE_DOMAIN}
#    - pd8.${CLOUDFLARE_DOMAIN}
#    - pd9.${CLOUDFLARE_DOMAIN}
#    - pd10.${CLOUDFLARE_DOMAIN}
#
# 2. Make sure all proxy workers are accessible and working
#
# 3. Update the CLOUDFLARE_DOMAIN above with your actual domain
#
# ================================================